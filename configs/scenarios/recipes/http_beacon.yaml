version: 1
name: HTTP Beacon Recipe
description: Sends three timed HTTP requests through the generic HTTP primitive
on_error: stop

steps:
  # T1071.001 - Application Layer Protocol: Web Protocols
  - id: requests
    module: net_http
    params:
      target: http://example.com
      method: GET
      count: 3
      interval: 1
      jitter: 0
